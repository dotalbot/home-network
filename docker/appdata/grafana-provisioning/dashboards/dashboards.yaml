apiVersion: 1

providers:
  - name: home-network
    orgId: 1
    folder: Home Network
    folderUid: home-network
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 60
    options:
      path: /etc/grafana/provisioning/dashboards/json
      foldersFromFilesStructure: false
